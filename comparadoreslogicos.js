// COMPARADORES LOGICOS:

Nombre = "julian";

if ( nombre == "lucas" ) {
    alert("Hola")
} // se ejecuta en el html para verlo en terminal.
// if solo se coloca 1 igual q python, y pasa igual si ponemos dos if por mas q se cumpla el priemro se seguira ejecutando el segundo o los q tengamos y es al pedo, mejor poner un if else / elif.

else if ( nombre == "rodrigo"){
    alert("Tu nombre es" + nombre)

} // else if se colocan muchos como elif en python.


else {
    alert("El nombre no es el correcto.")
} // else se coloca solo uno como en python y solo si ninguna opcion se cumple.










































